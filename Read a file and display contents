#include <stdio.h>

int main() {
    FILE *fp = fopen("test.txt", "r");
    char ch;

    if (fp == NULL) {
        printf("File not found!");
        return 0;
    }

    while ((ch = fgetc(fp)) != EOF) {
        printf("%c", ch);
    }

    fclose(fp);
    return 0;
}
